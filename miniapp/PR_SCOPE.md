# PR scope

This PR bootstraps the canonical frontend and redesigns the read-only Home experience using existing API responses. It intentionally does not deploy the build or alter quick-action behavior without verified production handlers.
