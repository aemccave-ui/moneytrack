services:
  n8n:
    environment:
      MONEYTRACK_INVITE_TTL_SECONDS: "${MONEYTRACK_INVITE_TTL_SECONDS:?MONEYTRACK_INVITE_TTL_SECONDS is required}"
      MONEYTRACK_INVITE_BASE_URL: "${MONEYTRACK_INVITE_BASE_URL:?MONEYTRACK_INVITE_BASE_URL is required}"
